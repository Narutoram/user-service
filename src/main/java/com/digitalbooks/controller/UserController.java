package com.digitalbooks.controller;

import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.RestController;


@RestController
@RequestMapping("/api/student")
public class UserController {
	
	/*
	 * @Autowired StudentService studentService;
	 * 
	 * @PostMapping("/create") public StudentResponse createStudent (@RequestBody
	 * CreateStudentRequest createStudentRequest) { return
	 * studentService.createStudent(createStudentRequest); }
	 * 
	 * @GetMapping("getById/{id}") public StudentResponse getById (@PathVariable
	 * long id) { return studentService.getById(id); }
	 */
}
